/**
 * Configuración pública de la aplicación web de Firebase.
 *
 * Reemplaza `null` únicamente con la configuración real entregada por
 * Firebase Console para el proyecto Exclusive Shop. No se deben incluir
 * cuentas de servicio ni claves privadas en el frontend.
 */
export const firebaseConfig = null;
